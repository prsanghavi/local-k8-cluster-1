# provider-slack-conversation v2.0.1 omits ProviderConfigUsage permissions from
# its package metadata. Crossplane uses these objects to track a managed
# resource's reference to its ProviderConfig.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: provider-slack-conversation-providerconfigusage
rules:
  - apiGroups:
      - slack.crossplane.io
    resources:
      - providerconfigusages
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
  - apiGroups:
      - slack.crossplane.io
    resources:
      - providerconfigusages/status
    verbs:
      - get
      - update
      - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: provider-slack-conversation-providerconfigusage
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: provider-slack-conversation-providerconfigusage
subjects:
  - kind: ServiceAccount
    name: provider-slack-conversation-1ce26888ff09
    namespace: crossplane-system
