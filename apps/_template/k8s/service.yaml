apiVersion: v1
kind: Service
metadata:
  name: app-name  # TODO: replace
spec:
  selector:
    app: app-name  # TODO: replace
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
